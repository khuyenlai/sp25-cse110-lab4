1. It will print 3
2. It will print 150
3. It will print 150
4. It will return [50, 100, 150]
5. It will cause an error. In the for loop: for(let i=0; i< prices.length; i++>), i is declared using let, which is block-scoped. This means it only exists inside the for loop block, from { to }. Once the loop finishes, i goes out of the scope. So when the program reaches console.log(i);, it will throw an error.
6. It will cause an error because discountedPrice is declared with let inside the for loop, making it blocked-scoped and inaccessible outside the loop.
7. It will print 150
8. It will return [50, 100, 150]
9. It will return [50, 100, 150]
10. It will print 3
11. It will return [50, 100, 150]
12. 
A. student.name
B. student['Grad Year']
C. student.greeting()
D. student['Favorite Teacher'].name
E. student.courseLoad[0]
13.  
A. '32'
B. 1
C. 3
D. '3null'
E. 4
F. 0
G. '3undefined'
H. NaN
14.  
A. true
B. false
C. true
D. false
E. false
F. true
15. == compares values after performing type coercion if necessary. === compares both value and type. It is stric equality with no type conversion.
16.  *this is in part2-question16.js
17. The modifyArray function loops through [1, 2, 3], applying doSomething (which doubles the number) to each element. The results [2, 4, 6] are stored in a new array, which is returned. 
18. *this is in part2-question18.js
19. 
Output:
1
4
3
2
